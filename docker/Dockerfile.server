ARG GO_VERSION=1.25
ARG GOPROXY=https://goproxy.cn|https://proxy.golang.org|direct
FROM golang:${GO_VERSION}-alpine AS builder

ARG GOPROXY
ENV GOPROXY=${GOPROXY}

RUN apk add --no-cache git make

WORKDIR /build

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# Keep embedded Swagger/OpenAPI current whenever the server image is built.
RUN go run ./cmd/openapi && \
    go clean -cache && \
    CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo -o server ./cmd/server

FROM alpine:latest

RUN apk --no-cache add ca-certificates tzdata

ENV TZ=Asia/Shanghai

WORKDIR /app

COPY --from=builder /build/server .

RUN mkdir -p /var/log/gcs-distill

EXPOSE 8080

CMD ["./server", "-config", "/app/config.toml"]
